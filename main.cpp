#include<bits/stdc++.h>
#include<thread>
#include<opencv2/opencv.hpp>

int main (){
    std::this_thread::sleep_for(std::chrono::seconds(10));
    std::cout<<"NB"<<std::endl;
    cv::NamedWindow("test");
    string a="";
    return 0;
}